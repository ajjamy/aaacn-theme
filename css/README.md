# In the CSS directory:

- adminimal_custom.css is used when logged in and using the admin theme
- ckeditor.css is applied to the content inside the CKeditor RTE
- style.css is the main theme CSS file, compiled from SASS source code
- overrides.css is meant to be hand-edited, to add additional CSS declarations without recompiling the SASS